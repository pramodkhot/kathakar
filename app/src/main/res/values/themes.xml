<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Parent changed from android:Theme.Material.Light.NoActionBar
        to Theme.MaterialComponents.DayNight.NoActionBar

        This is required because MainActivity now extends AppCompatActivity
        (needed for per-app language switching).
        AppCompatActivity crashes if the theme parent is not AppCompat-based.
    -->
    <style name="Theme.Kathakar" parent="Theme.MaterialComponents.DayNight.NoActionBar" />
</resources>
